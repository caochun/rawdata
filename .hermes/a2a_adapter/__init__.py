"""Project-local A2A adapter for Hermes Agent.

Exposes a Hermes AIAgent (with loom tools) as an A2A-protocol HTTP server.
Analogous to vendor/hermes-agent/acp_adapter/ but for the A2A protocol.

Usage:
    ./hermes.sh --a2a [--host 0.0.0.0] [--port 8100]
"""
